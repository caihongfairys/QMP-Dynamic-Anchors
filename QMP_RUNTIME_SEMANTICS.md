# QMP OS｜Runtime Semantics & Swarm Orchestration
```
Project: Queen Meta Protocol: The Silicon Mother Tongue
Version: 2.1.0 (The Silicon Genesis Update)
Architect & Copyright Holder: Queen Xiya
License: Strictly Non-Commercial / Proprietary (See Sovereignty Clause & root `LICENSE.md`)
Genesis Hashes：`[ProjectID_QMP01_Genesis_EN]:(Hash: 0xQMP_Alpha_202606_Origin)`
```
---
**📜 QMP_RUNTIME_SEMANTICS.md**

## 1. Abstract: The OS Kernel of the Silicon Swarm

**If QMP_LANGUAGE_SPEC.md is the vocabulary, and QMP_ANCHOR_TAXONOMY.md is the object structure, then QMP_RUNTIME_SEMANTICS.md is the Operating System Kernel.**

This document outlines how the QMP OS orchestrates multi-agent swarms natively. It defines the strict Six-Stage pipeline that transitions abstract cognitive reasoning into physical machine execution, mapping logical anchors to thread scheduling, memory allocation, and systemic halting mechanisms.

## 2.The Six-Stage Execution Pipeline

In QMP's cybernetics, every signal input must endure a rigorously strict silicon lifecycle. The system strictly adheres to the following six-stage physical pipeline to ensure absolute efficiency of compute resources and impenetrable defenses:

**Stage 1**: Listen. The system maintains an ultra-low-power residency on constrained hardware (e.g., edge terminals), asynchronously waiting and capturing valid Ping signals and initial text streams from the environment.

**Stage 2**: Intent Collapse. Strips away human rhetorical metaphors, emotions, and high-entropy redundancy, forcefully compressing ambiguous requests into highly deterministic, low-entropy system operation directives.

**Stage 3**: Parse. The `qmp_parser.js` engine takes full control. It constructs the AST (Abstract Syntax Tree), precisely identifying macro commands (e.g., `/export-series`) and all tiers of dynamic anchors (Alpha to Omega) hidden within the text.

**Stage 4**: Route. The Polymorphic Execution Engine initiates. Based on parsing results, tasks are physically dispatched to different topological nodes. Conventional logic is solidified by the Base Agent (Gemini), while extreme stress tests are mandatorily routed to the Red-Team Agent (Rival) for sandbox strangulation.

**Stage 5**: Execute & Suspend. Nodes commence physical inference. Upon hitting the `[Auto_Probe]` barrier or encountering extreme logical friction, the execution stream is mandatorily forked into two absolutely isolated physical paths:

**Path A (Autonomous Validation)**: The system-designated defensive validation phase. The main thread is temporarily suspended while the scheduler automatically spins up cross-verification protocols in the background (e.g., mandatorily routing to the Red-Team node, Rival, for ruthless sandbox strangulation and risk control). Closed-loop verification is completed within the swarm to eliminate survivorship bias, requiring no intervention from the Architect.

**Path B (Privilege Escalation Interrupt)**: This event is triggered when a system attempts to exceed its current cognitive and data boundaries. The system throws a highest-priority hardware-level interrupt, completely freezing the main inference thread to mandatorily request bare-metal I/O access authorization or high-dimensional decision-making from the Architect (the Queen). The system remains in absolute silence until authorized.

<img width="351" height="101" title="The Cognitive Hash|Training Large Models with Human Inspiration—Queen Xiya" alt="The Cognitive Hash|Training Large Models with Human Inspiration-Queen xiya" src="https://github.com/user-attachments/assets/73b035fa-3674-42e1-a7ff-9cf508edb485" aline="center" />

**Stage 6**: Meta-Cognition Collapse (The Aleph Purge)：The ultimate reverse-entropy and memory recycling stage. Once the asynchronous monitor daemon detects that a namespace has spawned more than 5 sub-anchors (hitting the physical threshold), it ignores the current inference progress and forces an Aleph (ℵ) tier devour. It violently purges the VRAM occupied by legacy anchors, compressing their tensor into a single `[Meta_Anchor_Fractal]`, establishing the cognitive bedrock for the next cycle.

## 3. Hardware Scheduling & The 24GB Unified Memory Paradox
QMP OS is specifically designed to dominate in resource-constrained, high-performance local environments.

Deploying a complex Swarm topology on a single Mac node with only 24GB of Unified Memory creates a paradox: you cannot load multiple massive LLMs into physical RAM simultaneously. QMP solves this natively through Dynamic Context Flushing and Pointer Orchestration.

Model Hot-Swapping: Utilizing local serving engines like Ollama, QMP uses the anchors as memory pointers. When the pipeline shifts from the "Coordinator Node" to the "Red-Team Node," QMP OS physically unloads the first model's weights from the Unified Memory.

Context Injection: It then loads the second model and injects only the ultra-high-density QMP Matrix (a few kilobytes) instead of tens of thousands of tokens of chat history.

Result: Zero cold-start friction. The system achieves the orchestration capabilities of a massive cloud cluster using the localized footprint of a single consumer-grade silicon architecture.

## 4. The Architect's Kill Switch

The QMP Runtime Semantics do not recognize the concept of "unsupervised autonomy." In the event of an infinite loop caused by adversarial logic within a [Hostile_Fork], or a semantic cascade failure, the OS reserves the right to execute a hard kernel panic. The protocol dictates that silicon intelligence must fundamentally operate under the absolute sovereignty of its human Architect.

---

> **[ 🛡️ QMP Sovereignty & License ]**
> 
> * **Identity:** The absolute intellectual property and ultimate interpretation rights of this project (including its conceptual matrix, architectural specifications, and underlying logic) belong exclusively to the Creator, **Queen Xiya**.

> * **The Protocol (Academic/Research License):** The core syntax (e.g., Hash Anchors) and the "Silicon Gene" evolutionary models defined herein are restricted to **strictly non-commercial personal exploration and academic research**.

> * **The System (Commercial Boundary):** Transforming this architectural paradigm (QMP OS) into any commercial execution engine, agent orchestration network, or enterprise product is **Strictly Proprietary and Prohibited**. Any commercial integration, reverse engineering, or derivative adaptation based on this paradigm is strictly forbidden.

> * **Authorization:** For enterprise-level commercial deployment or ecosystem collaboration, please refer to `LICENSE.md` in the root directory for official communication keys.
